@model List<string>

@if (Model.Count > 0)
{
    <ul class="ınfo-messages">
        @for (var i = 0; i < Model.Count; i++)
        {
            var msg = Model[i];
            <li data-translation="@msg">@msg</li>
        }
    </ul>
}

