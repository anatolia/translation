@model InputModel

<div class="input">

    <label for="@Model.Name">@Localizer.Localize(Model.LabelKey)</label>
    <input type="text"
           id="@Model.Name"
           name="@Model.Name"
           value="@Model.Value"
           @Html.Raw(Model.IsRequired ? "required" : "") />

    @if (Model.ErrorMessage.IsNotEmpty())
    {
        <div class="error">@Model.ErrorMessage</div>
    }
</div>